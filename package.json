{
  "name": "tiara-stack-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "checks": "vp run -r format && vp run -r lint && vp run -r test",
    "format:apply": "vp run -r format:apply",
    "build": "vp run -r build",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@rocicorp/zero-sqlite3",
      "better-sqlite3"
    ],
    "patchedDependencies": {
      "effect@4.0.0-beta.43": "patches/effect@4.0.0-beta.43.patch"
    }
  }
}
